on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  full: 
    runs-on: ubuntu-latest      
    steps:
      - name: Checkout project
        uses: actions/checkout@v2

      - name: Run tests...
        run: time make tests
